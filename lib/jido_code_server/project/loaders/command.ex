defmodule JidoCodeServer.Project.Loaders.Command do
  @moduledoc """
  Placeholder command loader.
  """

  @spec load(map()) :: {:ok, list()} | {:error, term()}
  def load(_layout), do: {:ok, []}
end
