defmodule JidoCodeServer.Project.Loaders.Workflow do
  @moduledoc """
  Placeholder workflow loader.
  """

  @spec load(map()) :: {:ok, list()} | {:error, term()}
  def load(_layout), do: {:ok, []}
end
